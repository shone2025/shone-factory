---
description: SF管理端开发规则 - AI辅助开发时必须遵守
---

# SF管理端开发规则

## 🛡️ 版本保护原则

1. **已确认功能不可随意修改**
   - 任何已在VERSION文件中记录的功能，视为"已确认功能"
   - 修改已确认功能前，必须明确告知用户并获得同意
   - 如需修改，必须说明修改原因和可能的影响

2. **新功能不影响现有程序**
   - 添加新功能时，必须确保现有功能正常运行
   - 新功能应独立开发，使用独立的函数和API路由
   - 修改前先理解现有代码逻辑，不要盲目重写

## 📋 开发流程

1. **修改前**
   - 查看VERSION文件了解当前版本和已实现功能
   - 搜索相关代码，理解现有实现
   - 如涉及已确认功能，必须先征求用户同意

2. **修改中**
   - 优先使用现有函数和逻辑（如sync_to_cloud）
   - 不重复造轮子，复用已验证的代码
   - 每次修改后进行语法检查

3. **修改后**
   - 更新VERSION文件记录变更
   - 备份到versions目录
   - 重启服务器验证

## 🔒 关键功能锁定（绝对禁止修改）

以下功能经多次验证流程正确，**任何AI均不得修改**：

### 1. 获取登录码流程 (get_device_code)

- **文件**: `factory_auth_web.py`
- **函数**: `TokenManager.get_device_code`
- **关键时序（不可更改）**:
  - 启动droid后等待 **4秒**
  - 发送 `/login`，等待 **0.5秒** 后回车
  - 等待 **2秒** 后第二次回车
  - 等待 **2秒** 后第三次回车
- **禁止原因**: 时序已针对droid CLI精确调优，修改将导致流程失败

### 2. step2_capture_device_code 函数

- 与 `get_device_code` 流程基本一致
- 不可删除、合并或重写

> ⚠️ 如确需修改，必须先与用户确认，并提供回滚方案

---

## ⚠️ 禁止行为

- ❌ 未经同意删除或大幅修改已确认功能
- ❌ 调用不存在的API接口
- ❌ 重写整个函数而不理解原有逻辑
- ❌ 忽略用户之前确认的实现方式
- ❌ **修改上述🔒锁定功能的任何代码或时序**

## ✅ 推荐行为

- ✓ 参考旧版本代码（如 factory-auth-tool）
- ✓ 使用已验证的云端接口（/api/update）
- ✓ 增量修改而非全量重写
- ✓ 保持代码风格一致
